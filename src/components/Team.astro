---
import fg from "fast-glob";
const collections: string[] = fg.sync("src/assets/team/*");
function capitalizeFirstLetter(string: string) {
  return string.charAt(0).toUpperCase() + string.slice(1);
}
---

<h3 class="text-2xl font-semibold text-center text-blue-600">
  run with ❤️ for students by students
</h3>
<div class="flex flex-row my-10 overflow-x-auto scroll-smooth">
  {
    collections.map((image) => (
      <>
        <img
          src={image}
          class="w-h-20 mr-7 flex h-20 rounded-full border-4 hover:border-[#5884FB]"
        />
        <h1>
          {false &&
            capitalizeFirstLetter(
              image.replace("src/assets/team/", "").replace(".jpg", "")
            )}
        </h1>
      </>
    ))
  }
</div>

<style>
  /* Hide the verticle scroll bar */
  /* Chrome */
  *::-webkit-scrollbar {
    width: 10px;
  }
  /*Firefox and Edge*/
  * {
    scrollbar-width: none;
    -ms-overflow-style: none;
  }
</style>
