---
import fg from "fast-glob";
const collections: string[] = fg.sync("src/assets/team/*");

// let hover = false;
// function capitalizeFirstLetter(string) {
//   return string.charAt(0).toUpperCase() + string.slice(1);
// }
---

<div>
  <div class="flex flex-row my-10">
    {
      collections.map((image) => (
          <img
            src={image}
            class="mx-auto flex h-20 w-20 rounded-full border-4 border-[#5884FB]"
          />
          // <h1>
          //   {capitalizeFirstLetter(
          //     image.replace("src/assets/team/", "").replace(".jpg", "")
          //   )}
          // </h1>
      ))
    }
  </div>
  <hr class="mx-auto h-1 w-[95%] bg-[#2356DD] border-1" />
  <div class="flex flex-col float-left mx-[2.5%] text-[#2356DD]">
    <h3 class="text-1xl mb-1">McMaster University</h3>
    <h1 class="text-2xl font-bold">Computer Science</h1>
    <h1 class="text-2xl font-bold mb-3">Society</h1>
  </div>
  <div class="flex justify-end mx-9 text-[#2356DD]">
    <!-- Copyright with right align -->
    <h1 class="text-1xl font-bold mb-2">Copyright &#169; 2023</h1>
  </div>
</div>
